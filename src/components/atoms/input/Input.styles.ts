import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  text: {
    fontFamily: "Poppins",
  },
  input: {
    width: "100%",
    borderRadius: 4,
  },
});

export default styles;
